# Start: Velnor CI green everywhere

This is a fresh run of the goal defined in @GOAL.md. Everything in that file —
context, objectives, hard rules, operating procedure, escalation conditions,
done criteria — binds.

First action: step 1 of the operating procedure — run the baseline audit
(`scripts/audit.sh`), commit and push the control repo, then continue the loop
until the done criteria are met.
